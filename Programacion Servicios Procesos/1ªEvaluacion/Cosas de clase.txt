 ----------
|12/09/2024|
 ----------
-Buscamos una clase de java para manipular una ip, en la que se usa inetAdrress.
-ssl.
-Protocolo tcp.
-Clase server socket.
-Clase socket.
-Clase para manipular paquetes tcp en java.
-Puertos logicos.
-Todos los programas complejos trabajan con hilos.
-Diferencia entre preoceso e hilo.
-Ejemplo de cliente eco.

 ----------
|17/09/2024|
 ----------
-Nos unimos hoy al classroom de servicios y procesos.
-Subida la programacion didactica del modulo.
-La clase InetAddress.
-Nunca se instancia, se obtiene un objeto de esta clase().
-Subimos al classroom el primer ejemplo de la clase inet address.
-Copiamos el metodo main ya que nos subió el metodo de prueba metodos.
-En esta clase al introducir el nombre de nuestro equipo nos sale las direcciones de localhost
 y también con las de google.
-Tenemos que subir al classroom un recorte de ejecucion de main de InetAddress.
-El jueves damos URL, aprender a como instanciar.

 ----------
|19/09/2024|
 ----------
-Clase URL.
-Esta clase si se instancia.
-Metodos de la clase de URL.
-Probamos el primer ejemplo con las rutas a oracle.
-Hicimos dos ejemplos del pdf subido.
-En el segundo nos muestra el HTML de la pagina web pasada en  la URL.
-Aunque ejecutes no tienes acceso directo.
-Probamos con Cebem y con el Faro de Vigo.
-Nos manda una tarea, done usamos el segundo ejemplo, con un recurso.
-La tarea era subir un captura de lo que muestra por consola con dicho enlace a otro pagina
 en este caso lo hice con la pagina web de as Deportivo.
-Tambien usamos un FileWriter para crear un archivo de texto.
-Creamos el Ejemplo1Conexion.
-Apache, mysql, php.

 ----------
|23/09/2024|
 ----------
-Arrancamos el apache para algun ejemplo.
-Creamos una carpeta en xamp de url para el index.html.
-Tener que ver el error por el que no pasa.
-Siempre al actualizar hay que hacer un reload(reiniciar).
-Estamos con el tercer ejemplo, en el cual el php va.

 ----------
|26/09/2024|
 ----------
-Que son los sockets.
-Conexion entre servidor y cliente.
-Tipos de sockets(Orientados a conexion).
-Clases para sockets TCP.
-Creamos la carpeta TCPintro, hacemos la primera clase Server1.
-Hay que echarle un ojo a la clase socket de la pagina 18.
-Ver tanto los constructores, como los metodos.
-Al hacer el Cliente1, tenemos que ejecutar el Server1 para que esté arrancado y despues ejecutar ya el cliente1.
-Seria bueno conectarse a otro servidor siendo clientes nosotros.
-Intentar conectarse desde este ordenador al de casa.
-Hay que ver para desactivar

 ----------
|30/09/2024|
 ----------
-Gestion de Sockets TCP.
-Creamos la clase Server2 y Cliente2.
-Copiamos algunos datos de las primeras clases y vemos desde el pdf para ir añadiendo.
-Vemos coomo mandar un mensaje al cliente.
-Funciona bien el programa.
-Si nos vamos a conectar a otro, siemore arranca el otro el server, y nosotros nos conectamos a su direccion.
-Hacer una variación, haciendolo en el ejercicio3.
-Conseguimos mandar el mensaje del cliente al servidor y que salga en mayusculas con el toUpperCase.

 ----------
|03/10/2024|
 ----------
-Vamos a continuar con los mensajes entre servidor y cliente.
-Creamos el paquete TCPFlujoContinuo.
-Y el fichero de ServerContinuo.
-Creamos un servidor eco, donde pasaremos continuamente un mensaje al servidor desde el cliente cuando nos conectemos.
-Tarea tenemos que cuando el cliente pase el asterisco al servidor se cierren ambos.
-Tenemos que hacer que el servidor te suba la cadena a mayusculas.
-Creamos el paquete TCPChat.
-Copiamos las clases anteriores, pero les cambiamos a ServerChat y ClienteChat.

